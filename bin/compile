#!/usr/bin/env bash

set -e
set -x

rm -rf out
mkdir out
cljsc src/leech/server '{:optimizations :simple :pretty-print true :target :nodejs}' > out/leech.js
sed -i 's/require.call(null, "sys").print/require.call(null, "util").print/' out/leech.js
rm -rf out/cljs out/clojure out/leech
